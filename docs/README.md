# docs/

This folder contains documentation assets and figures used in the main README.

Current figures:

- `workflow.svg` — end-to-end ADVIS pipeline
- `preprocessing.svg` — safety-area preprocessing pipeline
- `VAE_GAN_Model.svg` — VAE-GAN model architecture
- `sample_results.svg` — example anomaly detection outputs

Keep large datasets and model checkpoints outside this folder.